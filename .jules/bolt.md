## 2024-05-22 - [Empty Repository Optimization]
**Learning:** In a new repository without application code, performance optimization shifts to "passive" infrastructure, such as `.gitignore` to prevent future bloat from large files (Roadmap ENV-002).
**Action:** Frame `.gitignore` creation as a "Repository Performance" task, explicitly linking it to preventing Git index degradation from planned large datasets.
